#include<stdio.h>
#include<stdlib.h>


void main(){ 
	int fatorial;
	int parametro;
scanf("%d", &fatorial);
printf("%d",parametro);
 parametro = 2 + fatorial;
 parametro = 4;
if (  > parametro3 E < fatorial2) {
}
else {
scanf("%d", &fatorial);
}
 parametro = 2 + fatorial;
printf("%d",parametro);
}